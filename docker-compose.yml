services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ora
    ports:
      - "5432:5432"
    volumes:
      - ora-postgres:/var/lib/postgresql/data

volumes:
  ora-postgres:
